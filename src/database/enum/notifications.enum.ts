export enum NotificationType {
  INFO = 'info',
  GENERAL = 'general',
  MENTION = 'mention',
  COMMENT = 'comment',
  REMINDER = 'reminder',
  FOLLOW = 'follow',
  ACTIVITY = 'activity',
  WORKSPACE = 'workspace',
}

export enum ReminderType {
  EXACT = 'exact',
  BEFORE_TIME = 'before_time',
}
